enum BookingStatus {
  pennding,
  confirmed,
  canceled,
}

enum BookingTypeEnum {
  hour,
  day,
  week,
  month,
  year,
  custom,
}

enum BookingAttributesEnum {
  requestUser,
  sportFacility,
  clubOwner,
  paymentIntent,
}

enum PaymentStatusType {
  pending,
  approved,
  inProcess,
  rejected,
  refunded,
  cancelled,
}
